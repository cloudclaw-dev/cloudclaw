# Simple runtime image using pre-built JAR
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY cloudclaw-app/target/cloudclaw-app-1.0.0.jar app.jar
VOLUME /app/data
EXPOSE 8080
ENV JAVA_OPTS=""
ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
